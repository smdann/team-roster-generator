class Intern extends Employee {
  school;

  constructor(name, id, email) {
    super(name);
    super(id);
    super(email);
    this.school = school;
  }
  getSchool() {

  }
  getRole() {
    // return intern
  }
}