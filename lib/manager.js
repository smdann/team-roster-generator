class Manager extends Employee {
  officeNumber;

  constructor(name, id, email) {
    super(name);
    super(id);
    super(email);
    this.officeNumber = this.officeNumber;
  }
  getRole() {
    // return manager
  }
}